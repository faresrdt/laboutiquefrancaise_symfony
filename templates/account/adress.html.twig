{% extends 'base.html.twig' %}

{% block title %}Mes adresse - La Boutique Française{% endblock %}

{% block content %}
    <a href="{{ path('account_adress_add')}}" class="btn btn-info btn-sm float-right">Ajouter une adresse</a>
    <h1>Mes Adresses</h1>
    C'est dans cet espace que vous allez pouvoir gérer toutes vos adresses. <br>
    <a href="{{ path('account')}}">Retour à mon compte</a>
    <hr>
    {% if app.user.adresses|length == 0 %}
        <p class="text-center">
            Vous n'avez pas encore ajouté d'adresse dans votre compte client. Pour en ajouter une <a href="{{ path('account_adress_add')}}">Cliquez ici</a>.
        </p>
    {% else %}

        <div class="row">
            {% for address in app.user.adresses%}
                <div class="col-md-4">
                    <div class="card" style="width: 18rem;">
                        <div class="card-body">
                            <h5 class="card-title">{{ address.name }}</h5>
                            <p class="card-text">{{ address.adress }} <br>
                            {{ address.city }}, {{ address.postal }}</p>
                            
                            <a href="{{ path('account_adress_edit', { 'id' : address.id }) }}" class="btn btn-info">Modifier</a>
                            <a href="{{ path('account_adress_delete', { 'id' : address.id }) }}" class="btn btn-danger">Supprimer</a>
                        </div>
                    </div>
                </div>
            {% endfor %}
        </div>
        
    {% endif %}
{% endblock %}



